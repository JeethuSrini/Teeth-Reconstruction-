# Requires Python 3.9-3.12 (open3d doesn't support 3.13+ yet)

# Core dependencies
trimesh>=4.0.0
open3d>=0.18.0
numpy>=1.24.0
scipy>=1.10.0

# Registration
probreg>=0.3.0      # GPU-accelerated CPD (uses CuPy backend)
pycpd>=2.0.0        # CPU fallback for CPD

# GPU acceleration (optional but recommended)
# Install the version matching your CUDA version:
#   CUDA 11.x: pip install cupy-cuda11x
#   CUDA 12.x: pip install cupy-cuda12x
# Or install generic: pip install cupy
cupy-cuda12x        # Adjust for your CUDA version

# Alternative GPU via PyTorch (optional)
# torch>=2.0.0
